# === Database Configuration ===
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:athenadb}
spring.datasource.username=${DB_USER:athena}
spring.datasource.password=${DB_PASS:e9383z}
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# === Server Port ===
server.port=8080
